import java.util.*;
import java.util.Arrays;
public class Number {
	//Input:{1,0,4,0,3,0} output:{1,4,3,0,0,0}
	public static void main(String[] args) {
		int [] input= {1,0,4,0,3,0};
		
		/*
		 * int [] result= Arrays.stream(input).filter(num->num!=0).toArray();
		 * 
		 * int zeroCount= input.length-result.length; result=Arrays.copyOf(result,
		 * input.length); System.out.println(Arrays.toString(result));
		 * Arrays.fill(result, input.length-zeroCount,input.length,0);
		 * 
		 * System.out.println(Arrays.toString(result));
		 */
		
		

	}

}
